This project implements a RESTful web service using Node.js, Express, and MongoDB.
The application performs CRUD operations on a Person collection with fields such as
Name, Age, Gender, and Mobile number.
REST endpoints include GET, POST, PUT, and DELETE methods.
